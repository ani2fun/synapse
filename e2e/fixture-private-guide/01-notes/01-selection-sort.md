---
title: Selection Sort, Rewritten
summary: Proves a private satellite lesson reaches its reader and nobody else.
---

# Selection Sort, Rewritten

The quokka invariant: after pass `i`, the first `i` slots hold the smallest values in order.

Served from the private satellite, grafted under `programming-languages`.
